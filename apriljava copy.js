//const beep = "Beep!"
//const boop = "Boop!"
//const beMy = "Won't you be my neighbor?"
//const botArray = ["Beep!", "Boop!", "Won't you be my neighbor?"]
//let userArray = []
//let amendedArray = []

//$(document).ready(function () {
//  $("#formOne").submit(function (e) {
//    e.preventDefault();
//    const personInput = parseInt($('input#userNum').val())
// });

// $(".refresh").click(function () {
//   location.reload()
// })
//});

//function beepBoop() {
//for (let i = 0; i < personInput.length; i++) {
//  let userArray = [personInput]
// return userArray
// }
//}


function beepBoop()


let userArray = []

function beepBoop(input) {
  for (let index = 0; index <= input; index += 1) {
    userArray = userArray + index;
  }
  userArray.replace(/[1]/g, "beep!")
  userArray.replace(/[2]/g, "boop!")
  userArray.replace(/[3]/g, "won't you be my neighbor?")
  console.log(userArray)
}


beepBoop(20)
userArray


$("#result").html(userString)


for (let index = 0; index <= input; index += 1) {
  userArray = userArray + index;
}






let userArray = []

function beepBoop(input) {
  for (let index = 0; index <= input; index += 1) {
    userArray = userArray + index;
  }
  string.replace(/[1]/g, "x");

}





let userArray = []

function beepBoop(input) {
  for (let index = 0; index <= input; index += 1) {
    userArray = userArray + index;
  }
  for (let i = 0; i <= input; i += 1) {

  }
}




let userArray = []

function beepBoop(input) {
  for (let index = 0; index <= input; index += 1) {
    userArray = userArray + index;
  }
}





function beepBoop(input) {
  let userArray = []
  for (let index = 0; index <= input; index += 1) {
    userArray = userArray + index;
  }
  userArray.array.forEach(function (element) {
    console.log(userArray)
  })
}



function beepBoop()


let userArray = []

function beepBoop(input) {
  for (let index = 0; index <= input; index += 1) {
    userArray = userArray + index;
  }
  userArray = userArray.replace(/[1]/g, "beep!")
  userArray = userArray.replace(/[2]/g, "boop!")
  userArray = userArray.replace(/[3]/g, "won't you be my neighbor?")
  console.log(userArray)
}



let userArray = []

function beepBoop(input) {
  for (let index = 0; index <= input; index += 1) {
    userArray = userArray + index;
  }
  userArray = userArray.replace(/[1]{10,}/g, "beep!")
  userArray = userArray.replace(/[2]{10,}/g, "boop!")
  userArray = userArray.replace(/[3]{10,}/g, "won't you be my neighbor?")
  console.log(userArray)
}



let userArray = []

function beepBoop(input) {
  for (let index = 0; index <= input; index += 1) {
    userArray = userArray + index;
  }
  userArray = userArray.replace(/[1]/g, "beep!")
  userArray = userArray.replace(/[2]/g, "boop!")
  userArray = userArray.replace(/[3]/g, "won't you be my neighbor?")
}

let userArray = []

function beepBoop(input) {
  for (let index = 0; index <= input; index += 1) {
    userArray = userArray + index;
  }
  userArray = userArray.replace(/[1]{1,}/g, "beep!")
  userArray = userArray.replace(/[2]{1,}/g, "boop!")
  userArray = userArray.replace(/[3]{1,}/g, "won't you be my neighbor?")
}//these are almost good regexs
//i like this too
[...Array(10).keys()]//comes back"1234567"

Array.from(String(12345), Number);//comes back"1234567"

let userArray = [...Array(input).keys()]